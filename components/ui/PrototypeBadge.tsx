import React from "react";

export function PrototypeBadge({ show }: { show?: boolean }) {
  // Floating bottom-right prototype badge removed as per client feedback.
  // Subtle attribution added to Footer instead.
  return null;
}
